% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-model.R
\name{model-method-optimize}
\alias{model-method-optimize}
\alias{optimize}
\title{Run optimization}
\arguments{
\item{data}{(named list) Values for all or part of the data \code{block} in the
Stan program. Each element is named after the corresponding Stan
variable and shaped as follows:\tabular{ll}{
   Stan type \tab R shape \cr
   \code{complex} \tab complex scalar (e.g. \code{1+2i}) \cr
   \code{complex_vector[n]}, \code{complex_row_vector[n]}, \verb{array[n] complex} \tab complex vector of length \code{n} \cr
   \code{complex_matrix[r, c]} \tab complex matrix, \code{r} x \code{c} \cr
   \code{tuple(T1, ..., Tk)} \tab \strong{unnamed} list of length \code{k}, element \code{j} shaped as \code{Tj} \cr
   \verb{array[n] tuple(...)} \tab list of \code{n} tuple-shaped lists \cr
   \verb{array[n, m] tuple(...)} \tab list (over the first index) of lists (over the second index) of tuples \cr
}


A length-1 R vector is read as a scalar, so a \code{vector[1]}, \verb{array[1] ...}, or length-1 tuple-slot container value must be given an explicit
shape (\code{as.array(x)} or \code{dim(x) <- 1L}) to match its declared dimension.

Real-valued input is accepted wherever a \code{complex} type is declared --
it is coerced automatically, including inside tuple slots. All other
Stan types use their ordinary R representation (numeric/integer scalars,
vectors, matrices, arrays).

As a power-user escape hatch, tuple variables can instead be supplied
pre-flattened using dotted names (e.g. \code{list(t.1 = ..., t.2 = ...)} for
a tuple \code{t}), bypassing the list-shape conversion above; this is not the
primary documented path. One caveat applies: a dotted entry for a
\code{complex} tuple slot that omits the internal \code{newstan_array_dims}
attribute is assumed to hold exactly one complex value per enclosing
array element, which is only correct when that slot is itself a plain
(non-container) \code{complex} -- for anything else (e.g. a
\code{complex_vector} tuple slot), use the list shapes above instead.}

\item{init}{Initial values for parameters. Either a non-negative number,
used as the range of CmdStan-style random initialization
(\code{Uniform(-init, init)} on the unconstrained scale; the default is 2),
or a named list / named numeric vector of constrained parameter values.
Supplied values are shared by all chains; parameters not covered by the
supplied values are randomly initialized with the default range. A named
list may include tuple-shaped list entries and R complex values for
\code{tuple(...)}/\code{complex} parameters, using the same canonical shapes
(and the same dotted-name escape hatch) documented for \code{data}.}

\item{algorithm}{(string) The optimization algorithm: \code{"lbfgs"}, \code{"bfgs"},
or \code{"newton"}.}

\item{jacobian}{(logical) Should the log density be adjusted by the
abs-determinant of the Jacobian of the inverse transformation?}

\item{init_alpha}{(number) Initial step size for LBFGS.}

\item{iter}{(integer) Maximum number of optimization iterations.}

\item{tol_obj}{(number) Absolute tolerance for changes in objective value.}

\item{tol_rel_obj}{(number) Relative tolerance for changes in objective value.}

\item{tol_grad}{(number) Absolute tolerance for the norm of the gradient.}

\item{tol_rel_grad}{(number) Relative tolerance for the norm of the gradient.}

\item{tol_param}{(number) Absolute tolerance for changes in parameter values.}

\item{history_size}{(integer) Number of corrections in LBFGS approximation.}

\item{save_iterations}{(logical) Should optimization iterations be saved?
When \code{TRUE}, \code{\link[=fit-method-draws]{$draws()}} on the returned \code{\link{StanMLE}}
exposes the full optimization path (one row per saved iteration,
including the initial point) instead of a single row for the final
estimate. \code{\link[=fit-method-mle]{$mle()}} is unaffected either way and always
reflects the final iteration.}

\item{num_threads}{(integer) The total number of threads to use across all
chains. Defaults to \code{-1} (all available threads).}

\item{opencl_ids}{(integer vector) \code{c(platform_id, device_id)} identifying
the OpenCL platform/device to run on. Only meaningful for a model
compiled with \code{use_opencl = TRUE} (see \code{\link[=stan_model]{stan_model()}}); errors if the
model was not compiled with OpenCL support. Defaults to \code{NULL}, meaning
\code{select_opencl_device()} is never called and the platform/device baked
in at compile time (0/0) is used.}
}
\value{
A \code{\link{StanMLE}} object containing the point estimate.
}
\description{
The \verb{$optimize()} method of a \code{\link{StanModel}} object runs Stan's
\code{"optimize"} method to find the maximum a posteriori (MAP) estimate or
maximum likelihood estimate (MLE). Returns a \code{\link{StanMLE}} object.
}
\seealso{
\code{\link[=model-method-sample]{$sample()}},
\code{\link[=model-method-laplace]{$laplace()}}

Other StanModel methods:
\code{\link{model-method-compile}},
\code{\link{model-method-diagnose}},
\code{\link{model-method-expose-stan-functions}},
\code{\link{model-method-generate-quantities}},
\code{\link{model-method-laplace}},
\code{\link{model-method-model-info}},
\code{\link{model-method-pathfinder}},
\code{\link{model-method-sample}},
\code{\link{model-method-variables}},
\code{\link{model-method-variational}}
}
\concept{StanModel methods}
