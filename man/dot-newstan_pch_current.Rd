% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pch.R
\name{.newstan_pch_current}
\alias{.newstan_pch_current}
\title{Return the on-disk path of the PCH currently memoized for \code{cppflags}.}
\usage{
.newstan_pch_current(cppflags)
}
\description{
Reconstructs the exact memo key \code{.newstan_pch_flags()} uses for a given
\code{cppflags} (always with \code{rebuild = FALSE}, matching that function's own
key -- the memo represents steady-state resolved flags regardless of how
they were resolved) and returns the associated PCH path, if any. Used by
\code{.compile_stan_model_environment()} (R/stan_model.R) to check whether a
just-failed compile's PCH is stale (and therefore worth rebuilding) without
duplicating the digest key construction there.
}
\details{
Returns \code{NA_character_} when no PCH is memoized for \code{cppflags} (either
\code{.newstan_pch_flags()} was never called with these flags in this session,
or it was and PCH was unavailable, e.g. no \code{make}).
}
\keyword{internal}
