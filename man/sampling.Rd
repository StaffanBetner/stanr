% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{sampling}
\alias{sampling}
\title{Run MCMC sampling on a Stan model}
\usage{
sampling(
  stanmod,
  data,
  num_warmup = 1000,
  num_samples = 1000,
  thin = 1,
  save_warmup = FALSE,
  num_chains = 1,
  id = 1,
  seed = NA,
  init = 2,
  algorithm = "hmc",
  engine = "nuts",
  metric = "diag_e",
  metric_file = NULL,
  stepsize = 1,
  stepsize_jitter = 0,
  max_depth = 10,
  int_time = 2 * pi,
  delta = 0.8,
  gamma = 0.05,
  kappa = 0.75,
  t0 = 10,
  init_buffer = 75,
  term_buffer = 50,
  window = 25,
  adapt_engaged = TRUE,
  refresh = 100,
  verbose = TRUE,
  num_threads = -1,
  ...
)
}
\arguments{
\item{stanmod}{A model environment returned by \code{\link[=stan_model]{stan_model()}}.}

\item{data}{Named list of data variables to pass to the model.}

\item{num_warmup}{Number of warmup iterations (default: 1000).}

\item{num_samples}{Number of post-warmup samples (default: 1000).}

\item{thin}{Thinning interval (default: 1).}

\item{save_warmup}{Save warmup samples (default: FALSE).}

\item{num_chains}{Number of parallel chains (default: 1). Chains are run in
parallel via TBB \code{parallel_for} inside the Stan services.}

\item{id}{Starting chain ID for RNG advancement (default: 1).}

\item{seed}{Random seed (NA = random).}

\item{init}{Initialization radius, or named constrained initial values
(default: 2).}

\item{algorithm}{Sampler algorithm: \code{"hmc"} or \code{"fixed_param"} (default: \code{"hmc"}).}

\item{engine}{HMC engine: \code{"nuts"} or \code{"static"} (default: \code{"nuts"}). Only
used when \code{algorithm = "hmc"}.}

\item{metric}{Euclidean metric: \code{"unit_e"}, \code{"diag_e"}, or \code{"dense_e"}
(default: \code{"diag_e"}).}

\item{metric_file}{Precomputed inverse metric. For \code{"diag_e"}, a numeric vector
of length equal to the number of unconstrained parameters. For \code{"dense_e"},
a square matrix. Can be a single metric (recycled across chains) or a list
of metrics (one per chain). Corresponds to CmdStan's \code{metric_file} argument.
Default: \code{NULL} (identity metric).}

\item{stepsize}{Initial stepsize (default: 1).}

\item{stepsize_jitter}{Uniform jitter for stepsize (default: 0).}

\item{max_depth}{Maximum tree depth for NUTS (default: 10). Not used
for static HMC.}

\item{int_time}{Integration time for static HMC (default: \code{2 * pi}). Not used
for NUTS.}

\item{delta}{Target acceptance rate (default: 0.8).}

\item{gamma}{Adaptation gamma (default: 0.05).}

\item{kappa}{Adaptation kappa (default: 0.75).}

\item{t0}{Adaptation t0 (default: 10).}

\item{init_buffer}{Warmup buffer width (default: 75).}

\item{term_buffer}{Warmup terminal buffer (default: 50).}

\item{window}{Adaptation window size (default: 25).}

\item{adapt_engaged}{Enable stepsize/metric adaptation during warmup
(default: TRUE). When FALSE, uses fixed stepsize throughout.}

\item{refresh}{Output refresh frequency (iterations between log messages,
default: 100).}

\item{verbose}{Print progress messages (default: TRUE).}

\item{num_threads}{Number of threads, or \code{-1} for all available threads.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A list containing:
\itemize{
\item \code{draws}: a \code{posterior::as_draws_df} object with parameter draws.
\item \code{diagnostics}: a \code{posterior::as_draws_df} object with sampler diagnostics.
\item \code{return_code}: integer status code.
\item \code{args}: named list of sampling arguments.
}
}
\description{
Run MCMC sampling on a Stan model
}
