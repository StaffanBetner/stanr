% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizing.R
\name{optimizing}
\alias{optimizing}
\title{Run optimization on a Stan model}
\usage{
optimizing(
  stanmod,
  data,
  algorithm = "lbfgs",
  iter = 2000,
  init_alpha = 0.001,
  tol_obj = 1e-12,
  tol_rel_obj = 10000,
  tol_grad = 1e-08,
  tol_rel_grad = 1e+07,
  tol_param = 1e-08,
  history_size = 5,
  seed = NA,
  id = 1,
  init = 2,
  save_iterations = FALSE,
  refresh = 100,
  verbose = TRUE,
  num_threads = -1,
  ...
)
}
\arguments{
\item{stanmod}{A model environment returned by \code{\link[=stan_model]{stan_model()}}.}

\item{data}{Named list of data variables to pass to the model.}

\item{algorithm}{Optimization algorithm: \code{"newton"}, \code{"bfgs"}, or \code{"lbfgs"}
(default: \code{"lbfgs"}).}

\item{iter}{Maximum iterations (default: 2000).}

\item{init_alpha}{Initial step size (default: 0.001).}

\item{tol_obj}{Tolerance on absolute objective changes (default: 1e-12).}

\item{tol_rel_obj}{Tolerance on relative objective changes (default: 1e4).}

\item{tol_grad}{Tolerance on gradient norm (default: 1e-8).}

\item{tol_rel_grad}{Tolerance on relative gradient norm (default: 1e7).}

\item{tol_param}{Tolerance on parameter changes (default: 1e-8).}

\item{history_size}{L-BFGS history size (default: 5).}

\item{seed}{Random seed (NA = random).}

\item{id}{Chain ID for RNG advancement (default: 1).}

\item{init}{Initialization radius, or named constrained initial values
(default: 2).}

\item{save_iterations}{Save all iterations (default: FALSE).}

\item{refresh}{Output refresh frequency (default: 100).}

\item{verbose}{Print progress (default: TRUE).}

\item{num_threads}{Number of threads, or \code{-1} for all available threads.}

\item{...}{Unused.}
}
\value{
A list containing:
\itemize{
\item \code{par}: named numeric vector of parameter values at the mode.
\item \code{value}: log probability at the mode.
\item \code{return_code}: integer status code.
\item \code{args}: named list of optimization arguments.
}
}
\description{
Run optimization on a Stan model
}
