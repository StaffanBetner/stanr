% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stanmodel-sample.R
\name{model-method-sample}
\alias{model-method-sample}
\alias{sample}
\title{Run MCMC sampling}
\arguments{
\item{data}{(named list) Values for all or part of the data \code{block} in the
Stan program. Each element is named after the corresponding Stan
variable and shaped as follows:\tabular{ll}{
   Stan type \tab R shape \cr
   \code{complex} \tab complex scalar (e.g. \code{1+2i}) \cr
   \code{complex_vector[n]}, \code{complex_row_vector[n]}, \verb{array[n] complex} \tab complex vector of length \code{n} \cr
   \code{complex_matrix[r, c]} \tab complex matrix, \code{r} x \code{c} \cr
   \code{tuple(T1, ..., Tk)} \tab \strong{unnamed} list of length \code{k}, element \code{j} shaped as \code{Tj} \cr
   \verb{array[n] tuple(...)} \tab list of \code{n} tuple-shaped lists \cr
   \verb{array[n, m] tuple(...)} \tab list (over the first index) of lists (over the second index) of tuples \cr
}


A length-1 R vector is read as a scalar, so a \code{vector[1]}, \verb{array[1] ...}, or length-1 tuple-slot container value must be given an explicit
shape (\code{as.array(x)} or \code{dim(x) <- 1L}) to match its declared dimension.

Real-valued input is accepted wherever a \code{complex} type is declared --
it is coerced automatically, including inside tuple slots. All other
Stan types use their ordinary R representation (numeric/integer scalars,
vectors, matrices, arrays).

As a power-user escape hatch, tuple variables can instead be supplied
pre-flattened using dotted names (e.g. \code{list(t.1 = ..., t.2 = ...)} for
a tuple \code{t}), bypassing the list-shape conversion above; this is not the
primary documented path. One caveat applies: a dotted entry for a
\code{complex} tuple slot is assumed to hold exactly one complex value per
enclosing array element, which is only correct when that slot is itself
a plain (non-container) \code{complex} -- for anything else (e.g. a
\code{complex_vector} tuple slot), use the list shapes above instead.}

\item{seed}{(integer) The random seed for reproducibility.}

\item{refresh}{(integer) How often (in iterations) to print progress.}

\item{init}{Initial values for parameters. Either \code{NULL} (equivalent to
the default radius of 2), a non-negative number, used as the range of
CmdStan-style random initialization (\code{Uniform(-init, init)} on the
unconstrained scale; the default is 2), or a named list / named numeric
vector of constrained parameter values.
Supplied values are shared by all chains; parameters not covered by the
supplied values are randomly initialized with the default range. A named
list may include tuple-shaped list entries and R complex values for
\code{tuple(...)}/\code{complex} parameters, using the same canonical shapes
(and the same dotted-name escape hatch) documented for \code{data}.}

\item{chains}{(integer) The number of MCMC chains.}

\item{chain_ids}{(integer vector) The IDs for each chain.}

\item{num_threads}{(integer) The total number of threads to use across all
chains. Defaults to \code{RcppParallel::defaultNumThreads()} (all available threads).}

\item{iter_warmup}{(integer) The number of warmup iterations.}

\item{iter_sampling}{(integer) The number of sampling iterations.}

\item{save_warmup}{(logical) Should warmup samples be saved? Ignored when
\code{fixed_param = TRUE}, since that mode runs no warmup and so has nothing
to save.}

\item{thin}{(integer) Thin interval.}

\item{max_treedepth}{(integer) Maximum tree depth for NUTS.}

\item{adapt_engaged}{(logical) Should step size adaptation be used?}

\item{adapt_delta}{(number) Target acceptance statistic for HMC.}

\item{step_size}{(number) Step size for HMC.}

\item{metric}{Character string indicating the metric to use: \code{"diag_e"},
\code{"dense_e"}, or \code{"unit_e"}.}

\item{inv_metric}{(numeric) Initial inverse mass matrix values.}

\item{init_buffer}{(integer) Adaptation phase: initial buffer length.}

\item{term_buffer}{(integer) Adaptation phase: terminal buffer length.}

\item{window}{(integer) Adaptation phase: window length.}

\item{fixed_param}{(logical) Treat all parameters as fixed (no adaptation).}

\item{show_messages}{(logical) When \code{TRUE} (the default), print progress
and informational output (e.g. iteration and timing messages) to the
console. Set to \code{FALSE} to silence it. Suppressed output is still
recorded and available via the fit's \verb{$output()} method.}

\item{show_exceptions}{(logical) When \code{TRUE} (the default), print
informational messages about numerical exceptions -- e.g. Metropolis
proposal rejections and rejected initial values. Set to \code{FALSE} to
silence them. Suppressed messages are still recorded and available via
the fit's \verb{$output()} method.}

\item{diagnostics}{(character vector) Which sampler diagnostics to check
immediately after sampling, printing a warning message for any problems
found -- see \code{\link[=fit-method-mcmc]{$diagnostic_summary()}}. One or more of
\code{"divergences"}, \code{"treedepth"}, and \code{"ebfmi"}. The default checks all
three; \code{NULL} or \code{""} skips the check entirely. Ignored when
\code{fixed_param = TRUE}.}

\item{engine}{(string) The sampling engine: \code{"nuts"}, \code{"static"}, or
\code{"walnuts"} (see \href{https://github.com/flatironinstitute/walnutpie}{walnutpie}).
\code{walnuts} only supports \code{metric = "diag_e"} and \code{adapt_engaged = TRUE}.
It reuses \code{max_treedepth} as the maximum trajectory doublings,
\code{adapt_delta} as the step size acceptance target, and \code{step_size} as the
initial step size; \code{refresh}, \code{step_size_jitter}, \code{init_buffer},
\code{term_buffer}, \code{window}, and \code{adapt_gamma}/\code{adapt_kappa}/\code{adapt_t0} are
ignored, and no sampler diagnostics or progress output are produced.}

\item{int_time}{(number) Integration time for static HMC.}

\item{step_size_jitter}{(number) Jitter for step size after adaptation.}

\item{adapt_gamma}{(number) Adaptation hyperparameter for dual averaging.}

\item{adapt_kappa}{(number) Adaptation hyperparameter for dual averaging.}

\item{adapt_t0}{(number) Adaptation hyperparameter for dual averaging.}

\item{max_step_halvings}{(integer) \code{walnuts} only: maximum number of step
size halvings per macro step.}

\item{min_micro_steps}{(integer) \code{walnuts} only: minimum number of micro
steps per macro step.}

\item{max_hamiltonian_error}{(number) \code{walnuts} only: maximum allowed
error in the Hamiltonian at a macro step.}

\item{mass_init_count}{(number) \code{walnuts} only: initial count for the
mass matrix estimator.}

\item{mass_additive_smoothing}{(number) \code{walnuts} only: additive
smoothing for the mass matrix estimator.}

\item{max_macro_steps_target}{(number) \code{walnuts} only: target number of
macro steps per iteration.}

\item{step_learning_rate}{(number) \code{walnuts} only: learning rate for the
Adam step size adaptation.}

\item{step_gradient_decay}{(number) \code{walnuts} only: gradient decay rate
for the Adam step size adaptation.}

\item{step_sq_gradient_decay}{(number) \code{walnuts} only: squared gradient
decay rate for the Adam step size adaptation.}

\item{step_stabilization}{(number) \code{walnuts} only: stabilization term for
the Adam step size adaptation.}

\item{step_learn_rate_decay}{(number) \code{walnuts} only: learning rate decay
exponent for the Adam step size adaptation.}

\item{opencl_ids}{(integer vector) \code{c(platform_id, device_id)} identifying
the OpenCL platform/device to run on. Only meaningful for a model
compiled with \code{use_opencl = TRUE} (see \code{\link[=stan_model]{stan_model()}}); errors if the
model was not compiled with OpenCL support. Defaults to \code{NULL}, meaning
\code{select_opencl_device()} is never called and the platform/device baked
in at compile time (0/0) is used.}
}
\value{
A \code{\link{StanMCMC}} object containing posterior draws and diagnostics.
}
\description{
The \verb{$sample()} method of a \code{\link{StanModel}} object runs Stan's
\code{"sample"} method, which uses Hamiltonian Monte Carlo (HMC) with the
No-U-Turn Sampler (NUTS) to draw from the posterior distribution.
Returns a \code{\link{StanMCMC}} object.
}
\seealso{
\code{\link[=model-method-optimize]{$optimize()}},
\code{\link[=model-method-variational]{$variational()}}

Other StanModel methods:
\code{\link{model-method-check-syntax}},
\code{\link{model-method-compile}},
\code{\link{model-method-diagnose}},
\code{\link{model-method-expose-stan-functions}},
\code{\link{model-method-format}},
\code{\link{model-method-generate-quantities}},
\code{\link{model-method-laplace}},
\code{\link{model-method-model-info}},
\code{\link{model-method-optimize}},
\code{\link{model-method-pathfinder}},
\code{\link{model-method-variables}},
\code{\link{model-method-variational}}
}
\concept{StanModel methods}
