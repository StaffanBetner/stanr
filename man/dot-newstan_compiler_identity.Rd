% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pch.R
\name{.newstan_compiler_identity}
\alias{.newstan_compiler_identity}
\title{Return an identity string for the active C++ compiler toolchain.}
\usage{
.newstan_compiler_identity()
}
\description{
Runs \verb{$(CXX) --version} through the same \code{make}-based probe used to pick
PCH flags below, and memoizes the result for the life of the R session
(single key -- the toolchain cannot change mid-session).
}
\details{
Used for two purposes: selecting PCH flags in \code{.newstan_pch_flags()}
(clang vs gcc), and as a component of \code{model_hash}
(\code{.compile_stan_model_environment()}, R/stan_model.R). The latter exists
because \code{Rcpp::sourceCpp()}'s own on-disk cache is keyed purely on the
source file's path/content identity plus whether a previously built
shared object still exists at its recorded path (see
\code{Rcpp:::.sourceCppFindCacheEntryIndex()}) -- it has no notion of compiler
identity or version. An in-place toolchain upgrade that leaves
\code{R.version$platform} unchanged (e.g. an Xcode Command Line Tools or
system gcc update) would therefore be invisible to sourceCpp's cache, and
-- now that \code{.compile_stan_model_environment()} uses a \emph{persistent}
cross-session cache dir instead of a per-session \code{tempdir()} -- a stale
\code{.so} built by the old toolchain could be \code{dyn.load}ed indefinitely.
Folding compiler identity into \code{model_hash} gives such an upgrade a new
cache entry instead.

Returns \code{""} if \code{make} is unavailable, mirroring \code{.newstan_pch_flags()}'s
own degrade path (PCH is unavailable under the same condition).
}
\keyword{internal}
