# newstan (development version)

## New features

- `StanModel$variables()` now returns variable metadata (types and dimensions) for each Stan model block by parsing the Stan code with `stanc` info output. (#??)
